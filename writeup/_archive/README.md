# Manuscript archive

Superseded copies of the manuscript, moved here 2026-07-30 during consolidation.

**The single live paper is `writeup/main/CAP_sleep_mask_manuscript_main.docx` — edit only that.**
Nothing here is edited going forward; these are frozen history. Note `.docx` is git-ignored,
so this folder is the *only* version history (there is no git undo).

Not archived here (kept separate on purpose): the professor-edited versions
`writeup/edits/CAP_sleep_mask_manuscript_V2_prof{,_typofix}.docx`.

## Backups of the canonical file (most recent first)

Each was captured *just before* the edit named in it, so it is the "undo" for that edit.

| File | Frozen state = "before …" | Superseded by |
|---|---|---|
| `CAP_sleep_mask_manuscript_main_PRE_SPINDLE_REFRAME_backup_20260723.docx` | before §4.4 was reframed from "spindle detection (negative)" to "a mechanical, not electrical, reflection" (lead with trial-averaged low-band increase; Figure 8 regenerated) | the spindle reframe (now live) |
| `CAP_sleep_mask_manuscript_main_PRE_SNR_DENSITY_backup_20260723.docx` | before Figure 2 SNR was changed from band-integrated to per-Hz **spectral-density** SNR (per-channel, positive on every channel/session) | the SNR-density edit |
| `CAP_sleep_mask_manuscript_main_PRE_SNR_backup_20260716.docx` | before the first SNR / Figure 2 revision (2026-07-16) | the initial SNR edit |
| `CAP_sleep_mask_manuscript_main_PRE_NEGATIVE_REMOVAL_backup_20260716.docx` | before the 2026-07-16 pass that trimmed/rebalanced the negative-result framing | that revision |
| `CAP_sleep_mask_manuscript_main_REDLINE_backup_20260625.docx` | the pre-accept version still carrying the tracked-change redline (28 ins / 19 del, author "Claude", 2026-06-25) that implemented the `writeup/edits/REVIEWER_PASS.md` peer-review fixes | accepting that redline into the canonical file (2026-07-16) |

## Older whole-manuscript versions (pre-`main` lineage)

| File | What it was |
|---|---|
| `CAP_sleep_mask_manuscript_v2.docx` | earlier v2 draft, before the `writeup/main/` canonical was established |
| `CAP_sleep_mask_manuscript.docx` | original whole-manuscript draft, superseded by the canonical |
